# Story 1.1: 2FA数据模型和后端基础设施

## Status
Approved

## Story
**As a** 系统管理员，
**I want** 系统具备存储和管理2FA相关数据的能力，
**so that** 为用户提供安全的双因素认证服务。

## Acceptance Criteria
1. 创建`user_two_factor`表，包含用户ID、密钥、备用码、启用状态等字段
2. 实现TOTP密钥生成和验证的核心逻辑，符合RFC 6238标准
3. 创建备用恢复码生成和验证机制
4. 实现数据库迁移脚本，支持平滑升级
5. 添加相关的数据访问层(DAO)和服务层代码

**集成验证**:
- IV1: 现有用户表结构保持不变，新表通过外键正确关联
- IV2: 现有用户认证流程不受影响，可正常登录
- IV3: 数据库性能测试确认新表不影响现有查询性能

## Tasks / Subtasks
- [ ] 数据库设计和迁移 (AC: 1, 4)
  - [ ] 设计`user_two_factor`表结构
  - [ ] 设计`user_two_factor_backup_codes`表结构
  - [ ] 创建数据库迁移脚本
  - [ ] 测试迁移脚本的向前和回滚操作
- [ ] TOTP核心逻辑实现 (AC: 2)
  - [ ] 实现TOTP密钥生成函数
  - [ ] 实现TOTP验证码生成函数
  - [ ] 实现TOTP验证码验证函数，支持时间窗口容错
  - [ ] 添加RFC 6238标准合规性测试
- [ ] 备用恢复码机制 (AC: 3)
  - [ ] 实现备用码生成算法
  - [ ] 实现备用码验证逻辑
  - [ ] 实现备用码加密存储机制
- [ ] 数据访问层实现 (AC: 5)
  - [ ] 创建TwoFactorAuth实体模型
  - [ ] 创建TwoFactorBackupCode实体模型
  - [ ] 实现TwoFactorDAO数据访问接口
  - [ ] 实现备用码DAO数据访问接口
- [ ] 服务层实现 (AC: 5)
  - [ ] 创建TwoFactorService业务逻辑接口
  - [ ] 实现2FA启用/禁用服务方法
  - [ ] 实现密钥和备用码管理服务方法
  - [ ] 添加服务层单元测试
- [ ] 集成验证测试 (AC: IV1, IV2, IV3)
  - [ ] 验证现有用户表结构完整性
  - [ ] 测试现有认证流程不受影响
  - [ ] 执行数据库性能基准测试

## Dev Notes

### 项目结构信息
[Source: architecture.md#High Level Architecture]
- 后端代码位于 `server/` 目录
- 采用分层架构：API层、业务逻辑层、数据访问层、数据模型层
- 基于 GoFrame v2.9.1 框架
- 数据库：MySQL + Redis

### 数据模型设计
[Source: architecture.md#Data Models]
基于现有的用户表结构，需要创建以下新表：

```sql
-- 双因素认证主表
CREATE TABLE user_two_factor (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    secret_key VARCHAR(255) NOT NULL,
    qr_code_url TEXT,
    is_active BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    verified_at DATETIME NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE,
    UNIQUE KEY uk_user_id (user_id)
);

-- 备用恢复码表
CREATE TABLE user_two_factor_backup_codes (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    code_hash VARCHAR(255) NOT NULL,
    is_used BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    used_at DATETIME NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE,
    INDEX idx_user_id (user_id)
);
```

### 技术实现要求
[Source: prd.md#技术约束和集成要求]
- 遵循 GoFrame 框架的分层架构模式
- 文件位置：`internal/logic/auth/twofa/`, `internal/model/entity/`, `api/admin/auth/`
- 命名约定：使用`TwoFA`或`twofa`前缀
- TOTP实现必须符合RFC 6238标准
- 密钥和备用码必须加密存储

### TOTP实现规范
[Source: prd.md#非功能需求]
- 验证码时间窗口：30秒
- 支持30秒的时钟偏移容错
- 密钥长度：32字节（Base32编码）
- 验证码长度：6位数字

### 安全要求
[Source: prd.md#技术约束和集成要求]
- 密钥使用AES-256加密存储
- 备用码使用bcrypt哈希存储
- 所有敏感操作记录安全日志
- 实现防暴力破解机制

### 性能要求
[Source: prd.md#非功能需求]
- 2FA验证过程应在3秒内完成响应
- 支持至少1000个并发用户同时进行2FA验证
- 新表设计不应影响现有查询性能

### 依赖的Go包
基于GoFrame项目结构，需要使用：
- `github.com/gogf/gf/v2` - GoFrame核心框架
- `github.com/pquerna/otp` - TOTP实现
- `golang.org/x/crypto/bcrypt` - 密码哈希
- `crypto/aes` - AES加密
- `encoding/base32` - Base32编码

### 文件组织结构
```
server/
├── internal/
│   ├── model/
│   │   └── entity/
│   │       ├── user_two_factor.go
│   │       └── user_two_factor_backup_codes.go
│   ├── dao/
│   │   ├── user_two_factor.go
│   │   └── user_two_factor_backup_codes.go
│   ├── logic/
│   │   └── auth/
│   │       └── twofa/
│   │           ├── twofa.go
│   │           └── backup_codes.go
│   └── service/
│       └── twofa.go
└── manifest/
    └── sql/
        └── migrations/
            └── 001_add_two_factor_auth.sql
```

## Testing

### 测试标准
[Source: architecture.md#测试策略]
- 单元测试覆盖率要求：>90%
- 测试文件位置：与源文件同目录，以`_test.go`结尾
- 使用GoFrame内置的测试框架
- 集成测试使用测试数据库

### 测试框架和模式
- 使用`github.com/gogf/gf/v2/test/gtest`进行单元测试
- 数据库测试使用事务回滚模式
- Mock外部依赖使用`github.com/golang/mock/gomock`

### 本故事特定测试要求
- TOTP算法正确性测试，包括边界情况
- 数据库迁移脚本测试
- 加密/解密功能测试
- 并发访问测试
- 性能基准测试

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-27 | 1.0 | 初始故事创建 | Scrum Master |